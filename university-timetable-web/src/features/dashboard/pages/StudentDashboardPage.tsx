import React from 'react';
import {Page} from '@/components/layout/Page';

export const StudentDashboardPage: React.FC = () => {
    return (
        <Page title="Кабинет студента">
            <p>Сводка по группам, курсам и расписанию (placeholder).</p>
        </Page>
    );
};
